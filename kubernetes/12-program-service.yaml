apiVersion: apps/v1
kind: Deployment
metadata:
  name: program-service
  namespace: new-event
spec:
  replicas: 2
  selector:
    matchLabels:
      app: program-service
  template:
    metadata:
      labels:
        app: program-service
    spec:
      containers:
        - name: program-service
          image: 790304250595.dkr.ecr.ap-southeast-1.amazonaws.com/program-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5001
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: new-event-secrets
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /ready
              port: 5001
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 5001
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: program-service
  namespace: new-event
spec:
  selector:
    app: program-service
  ports:
    - name: http
      port: 5001
      targetPort: 5001
  type: ClusterIP
