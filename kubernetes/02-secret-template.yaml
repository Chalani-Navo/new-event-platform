apiVersion: v1
kind: Secret
metadata:
  name: new-event-secrets
  namespace: new-event
type: Opaque
stringData:
  # Replace these placeholder values before applying this file.
  DATABASE_URL: "postgresql://eventuser:CHANGE_ME@YOUR_RDS_ENDPOINT:5432/eventdb"
  CLICKHOUSE_USER: "analytics_user"
  CLICKHOUSE_PASSWORD: "CHANGE_ME"
